// Default config for lineage test pipeline
params {
    samples     = ['HG001', 'HG002']
    read_length = 150
    num_reads   = 500
}

profiles {
    docker {
        docker.enabled = true
        process.container = 'python:3.11-slim'
    }
}
